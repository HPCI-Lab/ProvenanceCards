How many activities are present in the whole workflow?	The workflow described in the cards is focused on the training and release of the Nemotron-Cascade-2-30B-A3B model and its openNemo port. The main activities include data collection and curation, supervised fine-tuning (SFT), reinforcement learning (RL) training, model architecture implementation, quantization and QLoRA fine-tuning, benchmarking, and release/documentation. There are no explicit workflow diagrams or activity lists, but based on the cards, there are at least 6 major activities: data curation, SFT, RL training, model implementation/porting, quantization/fine-tuning, and benchmarking/release.
What is the final status of the workflow?	The final status of the workflow is successful completion and public release of the openNemo-Cascade-2-30B-A3B model, a pure-PyTorch, quantization- and QLoRA-compatible version of NVIDIA's Nemotron-Cascade-2-30B-A3B. The model is available on Hugging Face, with all weights and code released, and benchmark results reported. The workflow concludes with the model being ready for inference and fine-tuning by the community.
What is the time to completion of the workflow?	The exact time to completion of the workflow is not specified in the cards. However, the release date for the model and datasets is March 19, 2026. The training process for the SFT stage is reported to take approximately 33,000 steps (1.5 epochs), but no wall-clock time or total duration is provided for the entire workflow.
List all the parameters of the first activity of the workflow	The first activity is data curation and preparation. Parameters involved include: dataset sources (e.g., Nemotron-Math-v2, Nemotron-Science-v1, SWE-Gym), number of samples per domain, data format (JSONL), columns (e.g., prompt, responses_create_params, agent_ref), and curation methods (hybrid: human, synthetic, automated). No explicit hyperparameters are listed for this activity.
What hardware was used in the workflow?	The cards do not specify the exact hardware used for training or inference. However, for 4-bit quantized inference, the model can be loaded on consumer GPUs with 17 GB VRAM. Full-precision (bf16) inference requires ~65 GB VRAM. The original NVIDIA training likely used high-memory GPUs, but no specific hardware model is mentioned.
Who is responsible for this workflow (person or username or entity)?	The workflow is primarily attributed to NVIDIA (for the original model and datasets) and Empero AI (for the openNemo port). The main authors of the technical report are listed as Yang, Zhuolin; Liu, Zihan; Chen, Yang; Dai, Wenliang; Wang, Boxin; Lin, Sheng-Chieh; Lee, Chankyu; Chen, Yangyi; Jiang, Dongfu; He, Jiafan; Pi, Renjie; Lam, Grace; Lee, Nayeon; Bukharin, Alexander; Shoeybi, Mohammad; Catanzaro, Bryan; and Ping, Wei.
What was the specific execution order of the tasks?	The execution order is: (1) Data collection and curation, (2) Supervised fine-tuning (SFT) on packed sequences, (3) Reinforcement learning (RL) training with multi-domain and on-policy distillation data, (4) Model implementation and porting to pure PyTorch (openNemo), (5) Quantization and QLoRA fine-tuning, (6) Benchmarking and evaluation, (7) Public release of model and datasets.
List all parameters for all activites in the workflow	Parameters include: dataset sources and sample counts, SFT hyperparameters (batch size 64, sequence length 256K, max LR 5e-5, min LR 5e-6, warmup steps 200, cosine scheduler, AdamW optimizer, beta_1=0.9, beta_2=0.98, weight decay 0.1, 33,000 steps), RL dataset splits, model architecture (52 layers, 30.87B params, 23 MoE layers, 128 experts/layer, 6 selected/token, hidden size 2,688), quantization config (4-bit NF4, double quant, bfloat16 compute), and benchmarking settings (temperature=1.0, top_p=0.95).
What was the peak RAM consumption during the workflow?	Peak RAM (VRAM) consumption for full-precision (bf16) inference is ~65 GB. For 4-bit quantized inference, VRAM usage is ~17 GB. No explicit peak system RAM (CPU) usage is reported for training or data processing.
Has the model been trained in a distributed setting?	While not explicitly stated, the scale of the model (30B parameters, 256K sequence length, batch size 64) and the use of global batch size and optimizer settings strongly imply that distributed training (across multiple GPUs or nodes) was used for both SFT and RL stages.
What was the total power consumption in Watts of the GPU(s) during the workflow?	The cards do not provide any information about the total power consumption in Watts of the GPU(s) during the workflow. This information is not available.
What significant input artifacts are involved in the generation of the finetuned model?	Significant input artifacts include the curated SFT and RL datasets (Nemotron-Cascade-2-SFT-Data, Nemotron-Cascade-2-RL-data), which aggregate data from sources such as Nemotron-Math-v2, Nemotron-Science-v1, SWE-Gym, and others. These datasets are in JSONL format and contain millions of samples across math, science, chat, instruction following, safety, agentic, and software engineering domains.
What is the total energy use for completing the workflow?	The total energy use for completing the workflow is not reported in the cards. No kWh or energy consumption figures are provided.
List all input files with size larger than 100Mb	The cards do not list specific input files or their sizes. However, the SFT dataset is ~2.73 GB in total, and individual domain splits (e.g., Math, Science, General Chat) likely exceed 100 MB each, but no explicit file names or sizes are given.
List all different file types used as input	Input files are in JSONL (JSON Lines) format, as specified for the datasets. No other file types are mentioned for input.
Identify the largest output	The largest output is the final trained model checkpoint for Nemotron-Cascade-2-30B-A3B (and its openNemo port), which is approximately 65 GB in bf16 full precision, and about 17 GB when quantized to 4-bit NF4.
What is the science domain of the dataset?	The datasets cover multiple science domains, including mathematics, science (physics, chemistry, biology), software engineering, and general instruction following. The primary science domains are mathematics and STEM.
Does the dataset have a predetermined train-test split?	The datasets described are primarily for training (SFT and RL), and the cards do not mention a predetermined train-test split. The focus is on training data; evaluation is performed on external benchmarks.
How many samples are present in the whole dataset?	The total number of samples in the RL dataset is 73,809. The SFT dataset contains over 24 million samples (sum of all domains listed in the statistics table).
What is the data type of the ground truth (if present)?	The ground truth data type is text, as the datasets are in text modality and contain prompts, responses, and metadata in JSONL format.
What is the specific task for which the dataset was created?	The datasets were created for training large language models (LLMs) on instruction following, reasoning, math, science, code generation, agentic behavior, and safety tasks, to enable strong general-purpose and reasoning capabilities in the resulting model.
What is the size in byte of one sample?	The size of one sample is not explicitly stated, but given the total RL dataset size (~2.73 GB) and 73,809 samples, the average sample size is approximately 38 KB (2,730,000,000 bytes / 73,809 ≈ 36,980 bytes).
What is the total size of the whole dataset?	The RL dataset is approximately 2.73 GB in total. The SFT dataset is much larger, with over 24 million samples, but its total size is not specified.
What are the designed uses for this model?	The model is designed for general-purpose text generation, reasoning, mathematical problem solving, code generation, agentic workflows, instruction following, and alignment tasks. It supports both thinking (reasoning) and instruct (non-thinking) modes, and is suitable for research, fine-tuning, and deployment in various NLP applications.
How many epochs have been used in the finetuning?	The SFT model is reported to reach optimal performance after approximately 1.5 epochs over the packed training data.
How many model parameters (weights) does the model have?	The model has a total of 30.87 billion parameters, with approximately 3 billion active parameters per token due to the Mixture-of-Experts (MoE) architecture.
What is the science domain of the model?	The model is a general-purpose large language model with strong capabilities in mathematics, science (STEM), code, and reasoning, as well as general instruction following and agentic tasks.
What is the task solved by this model (regression or classification or forecast etc.)?	The model is designed for text generation (causal language modeling), instruction following, reasoning, and code generation. It is not a regression, classification, or forecasting model in the traditional sense.
What is the intended use of this model?	The intended use is for general-purpose text generation, reasoning, mathematical and scientific problem solving, code generation, agentic workflows, and research. It is suitable for both inference and further fine-tuning (e.g., with QLoRA).
What is the size of the final model in Mb?	The final model size is approximately 65,000 MB (65 GB) in bf16 full precision, and about 17,000 MB (17 GB) when quantized to 4-bit NF4.
What technique was used to fine-tune the model? (e.g. LoRa; GAN...)	The model was fine-tuned using supervised fine-tuning (SFT) and reinforcement learning (RL) with multi-domain and on-policy distillation data. QLoRA is supported for further fine-tuning, but not used in the original workflow.
What is the claimed performance of this model?	The model achieves gold medal performance on IMO 2025 (35 pts) and IOI 2025 (439.3), and strong results on AIME, HMMT, LiveCodeBench, ArenaHard v2, SWE Verified, and MMLU-Pro benchmarks, as reported in the technical report.
Are the performance shown in the pretrained version improved in the finetuning?	Yes, the performance of the model is improved through supervised fine-tuning and reinforcement learning, as evidenced by the strong benchmark results and gold medal achievements reported for the finetuned model.